server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # This line forces Nginx to route everything to Vue Router
        try_files $uri $uri/ /index.html;
    }
}
